
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: TEMPORARY DEBUGGING RULESET
    // This allows open access to your entire database.
    // This is ONLY to get the deployment to succeed.
    // We must revert to the secure rules once the deployment issue is resolved.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
